const pos = '#00cc66';
const neg = '#ff6347';

const colours = {
	pos,
	neg
};

const navBarHeight = '7.5';
const pagePadding = '5';

const sizing = {
	navBarHeight,
	pagePadding
};

export default {
	colours,
	sizing
};
